const initialState = {
    movies: [],
    message: 'Hello store'
}

export const movieReducer = (state = initialState , action:any) => {
    switch(action.type) {
        default: return state
 }
}



